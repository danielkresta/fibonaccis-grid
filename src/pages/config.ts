export const GRID_SIZE = {
  width: 50,
  height: 10,
};

export const COLOR_TIMEOUT_MS = 10;

export const SEARCHED_SEQUENCE_LENGTH = 5;

// It probably won't be necessary to support more than 377 clicks
export const GENERATED_SEQUENCE_LENGTH = 15;
